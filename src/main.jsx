import React from 'react';
import ReactDOM from 'react-dom/client';
import FleetManager from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FleetManager />
  </React.StrictMode>
);
